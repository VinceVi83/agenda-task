# agenda-task
A lightweight task orchestrator with a FastAPI backend and Streamlit frontend. It features an APScheduler engine with an agenda-style calendar timeline, custom CSS card monitoring, and a backend script parser that extracts MCP tools to schedule them and execute them in APScheduler engine.
